1、sudo su postgres  创建用户
   psql postgres  进入到此用户的数据库
   ALTER USER postgres with PASSWORD 'postgres'  更改密码
   create database votedb  owner postgres; 给用户创建数据库
   psql -U postgres -h 127.0.0.1 -d votedb;  链接该用户的votedb库
    GRANT ALL PRIVILEGES ON DATABASE mahjong TO postgres;  把数据库的所有权限给这个用户
    \dt  查看表   \l  查看多少库
   drop table alembic_version   删除某个表
   delete from 表明 清空表
2、p_gsql 导出数据 pg_dump -U postgres -t system_calls(表名) wangye(库名) > ./test.sql  
  导入数据：$ psql -d newdatabase -U postgres -f mydatabase.sql   // sql 文件在当前路径下
	 $psql -U zhao -d game -h localhost</home/yaya/Downloads/2018-01-02_14_09_14_test.sql// sql 文件不在当前路径下
	 sudo -u wjy sh -c 'psql game < /home/wangjianyu/2018-01-02_14:09:14_test.sql'


3、\d 表名 查看表结构
   \x 切换显示方式



